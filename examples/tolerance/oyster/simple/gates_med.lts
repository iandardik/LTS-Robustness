const Card1 = 0
const Card2 = 1
range AllCards = Card1 .. Card2

GI = (gatein.snd.oyster[c:AllCards] -> gatein.gin[c] -> GI).

GO = (gateout.snd.gin[c:AllCards] -> gateout.snd.bal[c] -> gateout.rec.newBal[c] -> GO).

||G = (GI || GO).
